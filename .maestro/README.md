# Mobile procedure-table flow

Run `.maestro/procedure-tables.yaml` on an installed phone build with Maestro and a running simulator/emulator. The flow uses the bundled snapshot, so no web server or mock API is required.
